<h1>Main#results</h1>
<p>Find me in app/views/main/results.html.erb</p>

<% @results.each do |item| %>
	<% if item["photos"] != nil && item["name"] != nil && item["price_level"] != nil && item["rating"] && item["vicinity"] != nil %>

				<div class="well">
				<h1><%= item["name"] %></h1>
				<p>Price Level: <%= item["price_level"] %></p>
				<p>Rating: <%= item["rating"] %></p>
				<p>Address: <%= item["vicinity"] %></p>
				<p>Photo ref: <img src="<%= @photo_prefix + item["photos"][0]["photo_reference"] + @key %>" /></p>
				</div>
	<% end %>
<% end %>



<h1>Cafes</h1>

<% @cafes.each do |item| %>
<div class="well">
	<h1><%= item["name"] %></h1>
	<p>Price Level: <%= item["price_level"] %></p>
	<p>Rating: <%= item["rating"] %></p>
	<p>Address: <%= item["vicinity"] %></p>

</div>
<% end %>

<h1>Bars</h1>

<% @bars.each do |item| %>
<div class="well">
	<h1><%= item["name"] %></h1>
	<p>Price Level: <%= item["price_level"] %></p>
	<p>Rating: <%= item["rating"] %></p>
	<p>Address: <%= item["vicinity"] %></p>

</div>
<% end %>

